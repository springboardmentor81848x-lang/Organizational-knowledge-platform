import React,{useEffect,useState} from 'react';import Sidebar from '../../components/Sidebar/Sidebar';import Navbar from '../../components/Navbar/Navbar';import {FaGraduationCap,FaCheckCircle,FaRoute,FaClock,FaArrowRight} from 'react-icons/fa';import {learningProgressAPI,learningPathAPI,analyticsAPI, getCurrentUserId } from '../../services/apiService';import './MyLearning.css';
export default function MyLearning(){const[items,setItems]=useState([]),[path,setPath]=useState(null),[stats,setStats]=useState(null);const load=async()=>{const[a,b,c]=await Promise.all([learningProgressAPI.get(getCurrentUserId()),learningPathAPI.getLearningPath(getCurrentUserId()),analyticsAPI.get(getCurrentUserId())]);if(a.success)setItems(a.data);if(b.success)setPath(b.data);if(c.success)setStats(c.data)};useEffect(()=>{load()},[]);const progress=async x=>{const p=Number(prompt('Enter new progress (0-100):',x.progress));if(Number.isNaN(p))return;await learningProgressAPI.updateProgress(x.id,p);await load()};return <div className="app-layout"><Sidebar/><div className="main-wrapper"><Navbar title="Learning Paths & Roadmaps" role="Employee"/><div className="page-container"><div className="page-header"><div className="page-header-text"><h2><FaRoute/> Structured Learning Roadmap & Progress</h2><p>Track enrollment, milestones, completion, certification and learning velocity.</p></div></div>{stats&&<div className="kpi-grid"><div className="kpi-card"><div className="kpi-content"><h3>{stats.enrolledCourses}</h3><p>Enrolled Courses</p></div><FaGraduationCap/></div><div className="kpi-card"><div className="kpi-content"><h3>{stats.completedCourses}</h3><p>Completed</p></div><FaCheckCircle/></div><div className="kpi-card"><div className="kpi-content"><h3>{stats.averageProgress}%</h3><p>Average Progress</p></div><FaArrowRight/></div></div>} {path&&<div className="card-box" style={{padding:24,marginBottom:24}}><h3>{path.title}</h3><p>Target role: <strong>{path.targetRole}</strong> • {path.totalDuration} • {path.estimatedTime}</p><ol>{(path.stages||[]).map(s=><li key={s.stage}><strong>{s.skill||s.title}</strong> — {s.severity||'Planned'} • {s.estimatedHours} hrs</li>)}</ol></div>}<div className="assessment-list">{items.length?items.map(x=><div key={x.id} className="card-box assessment-card"><div className="assessment-icon-wrapper"><FaGraduationCap/></div><div className="assessment-info"><h3>{x.title}</h3><p>{x.provider} • {x.skill_name} • {x.status}</p><div className="progress"><div className="progress-bar" style={{width:`${x.progress}%`}}></div></div><p><strong>{x.progress}%</strong> • {x.completed_modules}/{x.total_modules} modules</p></div><button className="btn btn-primary btn-sm" onClick={()=>progress(x)}>Update Progress</button></div>):<p>No enrollments yet. Visit Training to enroll.</p>}</div></div></div></div>}
